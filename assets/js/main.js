const toggle=document.querySelector(".menu-toggle");const nav=document.querySelector(".site-nav");if(toggle&&nav){toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));nav.dataset.open=String(!open)});nav.addEventListener("click",event=>{if(event.target.matches("a")){toggle.setAttribute("aria-expanded","false");nav.dataset.open="false"}})}document.querySelectorAll("[data-year]").forEach(item=>{item.textContent=new Date().getFullYear()});